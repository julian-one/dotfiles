#compdef custom
# Custom completion definitions
# Place any custom completion functions here

# Example: Git custom completions
# These will extend the built-in git completions

# Add more custom completions as needed
# To use: place completion files in this directory with names starting with underscore
# e.g., _mycommand for a command called 'mycommand'